import pandas as pd


def calc_summary_parameters(results: pd.DataFrame):
    """Calculate summary parameters from results time series

    :return:
    """
    summary = dict()


def determine_full_shift_cycles(scenario: str):
    pass